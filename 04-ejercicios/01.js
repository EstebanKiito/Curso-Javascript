// Retornar el mayor

function mayor(a, b) {
    return (a > b) ? a : b; // Recordar: (condicion) ? valor1 si true : valor2 si false la condicion (a > b)
}

let mayor = mayor(5, 10);
console.log(mayor); // 10